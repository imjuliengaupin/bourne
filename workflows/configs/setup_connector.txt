{
  // =========================
  // REQUIRED CONFIGURATION
  // =========================

  // 1. Source Data Connector
  // Choose a supported source data connector.
  // Supported values: local_file
  "source_type": "",

  // =========================
  // OPTIONAL CONFIGURATION
  // =========================

  // 1. Source Data Path
  // Path to your local data file or directory.
  // Applies to: local_file
  // Supported formats: json
  "source_path": "",

    // 2. Expected Data Schema
  // Define the expected schema for your data.
  // Connects to Pydantic Record objects in /bourne/schemas/.
  // Keys should be column names in UPPERCASE.
  // Values should be valid Python data types (see Python documentation).
  // Example: "expected_schema": {
  //   "COLUMN_NAME": "str",
  //   "COLUMN_NAME": "int",
  //   ...
  // }
  "expected_schema": {},

  // 3. Data Transformation Mode
  // Choose a transformation to apply to your data.
  // Supported values: lowercase_keys
  // Supported formats: json
  "transform_mode": "",

  // 4. Output Path for Transformed Data
  // Path to save transformed data.
  // Applies to: local_file
  // Supported formats: json
  "transform_output_path": ""
}
